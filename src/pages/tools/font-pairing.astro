---
import BaseLayout from '../../layouts/BaseLayout.astro';

// Comprehensive Industry Typographic Pairing Matrix (31 Designer-Engineered Combinations)
const pairings = [
  // 1. SaaS & Modern Tech
  {
    id: 'pair-space-inter',
    heading: 'Space Grotesk',
    headingCat: 'Sans-Serif',
    body: 'Inter',
    bodyCat: 'Sans-Serif',
    vibe: 'SaaS & Tech',
    contrastRule: 'Neo-brutalist tech headline balanced by ultra-clean neutral UI workhorse.',
    description: 'Distinctive retro-futuristic geometric stance anchored by high x-height screen legibility.'
  },
  {
    id: 'pair-jakarta-outfit',
    heading: 'Plus Jakarta Sans',
    headingCat: 'Sans-Serif',
    body: 'Outfit',
    bodyCat: 'Sans-Serif',
    vibe: 'SaaS & Tech',
    contrastRule: 'Contemporary Indonesian neo-grotesque paired with airy geometric loops.',
    description: 'Fresh, polished modern tech startup vibe favored by contemporary SaaS unicorns.'
  },
  {
    id: 'pair-sora-public',
    heading: 'Sora',
    headingCat: 'Sans-Serif',
    body: 'Public Sans',
    bodyCat: 'Sans-Serif',
    vibe: 'SaaS & Tech',
    contrastRule: 'Futuristic fintech geometry grounded by federal-grade US design system clarity.',
    description: 'Crisp, authoritative pairing engineered for data-dense dashboards and fintech interfaces.'
  },
  {
    id: 'pair-syne-dmsans',
    heading: 'Syne',
    headingCat: 'Display',
    body: 'DM Sans',
    bodyCat: 'Sans-Serif',
    vibe: 'SaaS & Tech',
    contrastRule: 'Explosive art-direction display punch balanced by compact geometric grotesque.',
    description: 'Trendsetting design agency and AI product aesthetic with immediate memorable impact.'
  },
  {
    id: 'pair-manrope-source',
    heading: 'Manrope',
    headingCat: 'Sans-Serif',
    body: 'Source Sans 3',
    bodyCat: 'Sans-Serif',
    vibe: 'SaaS & Tech',
    contrastRule: 'Semi-rounded geometric clarity paired with Adobe open-source humanist text engine.',
    description: 'Professional enterprise software stack offering superb micro-legibility at small viewport sizes.'
  },
  {
    id: 'pair-archivo-inter',
    heading: 'Archivo',
    headingCat: 'Sans-Serif',
    body: 'Inter',
    bodyCat: 'Sans-Serif',
    vibe: 'SaaS & Tech',
    contrastRule: 'Technical 19th-century industrial grotesk headline with digital UI neutral body.',
    description: 'Punchy technical architecture for developer tooling, cloud platforms, and CLI documentation.'
  },

  // 2. Editorial & High Luxury
  {
    id: 'pair-playfair-source',
    heading: 'Playfair Display',
    headingCat: 'Serif',
    body: 'Source Sans 3',
    bodyCat: 'Sans-Serif',
    vibe: 'Editorial & Luxury',
    contrastRule: 'High-contrast Didone fashion headlines combined with clean, legible humanist body.',
    description: 'The golden standard of luxury editorial magazines, premium lifestyle blogs, and high fashion.'
  },
  {
    id: 'pair-cormorant-montserrat',
    heading: 'Cormorant Garamond',
    headingCat: 'Serif',
    body: 'Montserrat',
    bodyCat: 'Sans-Serif',
    vibe: 'Editorial & Luxury',
    contrastRule: 'Renaissance calligraphic grace anchored by geometric architectural uppercase.',
    description: 'Breathtaking elegance for boutique hotels, fine fragrance, and artistic monographs.'
  },
  {
    id: 'pair-bodoni-raleway',
    heading: 'Bodoni Moda',
    headingCat: 'Serif',
    body: 'Raleway',
    bodyCat: 'Sans-Serif',
    vibe: 'Editorial & Luxury',
    contrastRule: 'Aristocratic razor-sharp unbracketed hairline serifs paired with airy humanist sans.',
    description: 'Vogue-style haute couture sophistication with ultra-refined typographic rhythm.'
  },
  {
    id: 'pair-cinzel-fauna',
    heading: 'Cinzel',
    headingCat: 'Serif',
    body: 'Fauna One',
    bodyCat: 'Serif',
    vibe: 'Editorial & Luxury',
    contrastRule: 'Roman monumental chisel inscription titles accompanied by gentle transitional serif.',
    description: 'Classical historical majesty for museums, prestigious law firms, and heritage vineyards.'
  },
  {
    id: 'pair-prata-lato',
    heading: 'Prata',
    headingCat: 'Serif',
    body: 'Lato',
    bodyCat: 'Sans-Serif',
    vibe: 'Editorial & Luxury',
    contrastRule: 'Teardrop Didone terminals balanced by warm Polish humanist proportions.',
    description: 'Graceful editorial poise with reliable, friendly body reading ergonomics.'
  },
  {
    id: 'pair-fraunces-urbanist',
    heading: 'Fraunces',
    headingCat: 'Serif',
    body: 'Urbanist',
    bodyCat: 'Sans-Serif',
    vibe: 'Editorial & Luxury',
    contrastRule: 'Warm wonky 1970s soft-serif display contrasted with strict geometric minimalism.',
    description: 'Modern artisanal luxury favored by organic DTC skincare and specialty coffee brands.'
  },

  // 3. Modern Minimalist & Clean
  {
    id: 'pair-montserrat-lora',
    heading: 'Montserrat',
    headingCat: 'Sans-Serif',
    body: 'Lora',
    bodyCat: 'Serif',
    vibe: 'Modern Minimalist',
    contrastRule: 'Geometric Buenos Aires street typography grounded by calligraphic literary serif.',
    description: 'Effortlessly chic Scandinavian balance between modern architectural clarity and warm prose.'
  },
  {
    id: 'pair-worksans-opensans',
    heading: 'Work Sans',
    headingCat: 'Sans-Serif',
    body: 'Open Sans',
    bodyCat: 'Sans-Serif',
    vibe: 'Modern Minimalist',
    contrastRule: 'Grotesque warm personality paired with universally tested accessibility standard.',
    description: 'Unfussy, utilitarian design system built for long-form newsletters and clean dashboards.'
  },
  {
    id: 'pair-jakarta-inter',
    heading: 'Plus Jakarta Sans',
    headingCat: 'Sans-Serif',
    body: 'Inter',
    bodyCat: 'Sans-Serif',
    vibe: 'Modern Minimalist',
    contrastRule: 'The Silicon Valley product standard: friendly display header + neutral body.',
    description: 'The exact design philosophy seen across Linear, Vercel, Raycast, and modern productivity suites.'
  },
  {
    id: 'pair-urbanist-newsreader',
    heading: 'Urbanist',
    headingCat: 'Sans-Serif',
    body: 'Newsreader',
    bodyCat: 'Serif',
    vibe: 'Modern Minimalist',
    contrastRule: 'Ultra-low contrast geometric sans paired with Production Type newsprint serif.',
    description: 'Clean digital journalism aesthetic engineered for immersive deep-reading experiences.'
  },
  {
    id: 'pair-epilogue-dmsans',
    heading: 'Epilogue',
    headingCat: 'Sans-Serif',
    body: 'DM Sans',
    bodyCat: 'Sans-Serif',
    vibe: 'Modern Minimalist',
    contrastRule: 'Contemporary brutalist variable grotesque balanced by circular friendly rhythm.',
    description: 'Modern architecture portfolio and creative portfolio benchmark with crisp geometric discipline.'
  },

  // 4. Creative Brand & Bold Agency
  {
    id: 'pair-oswald-merriweather',
    heading: 'Oswald',
    headingCat: 'Sans-Serif',
    body: 'Merriweather',
    bodyCat: 'Serif',
    vibe: 'Creative & Bold',
    contrastRule: 'Impactful condensed alternate gothics headline with robust, sturdy editorial serif.',
    description: 'Powerful editorial punch used by investigative journalism, investigative reports, and newsrooms.'
  },
  {
    id: 'pair-righteous-poppins',
    heading: 'Righteous',
    headingCat: 'Display',
    body: 'Poppins',
    bodyCat: 'Sans-Serif',
    vibe: 'Creative & Bold',
    contrastRule: 'Art deco dieselpunk display curves anchored by pristine Indian geometric circles.',
    description: 'Energetic video game interfaces, futuristic streetwear branding, and streaming platforms.'
  },
  {
    id: 'pair-unbounded-spline',
    heading: 'Unbounded',
    headingCat: 'Display',
    body: 'Spline Sans',
    bodyCat: 'Sans-Serif',
    vibe: 'Creative & Bold',
    contrastRule: 'Ultra-wide variable tech display paired with compact grotesque UI rhythm.',
    description: 'Cutting-edge Web3, crypto infrastructure, and hardware innovation visual identities.'
  },
  {
    id: 'pair-anton-roboto',
    heading: 'Anton',
    headingCat: 'Sans-Serif',
    body: 'Roboto',
    bodyCat: 'Sans-Serif',
    vibe: 'Creative & Bold',
    contrastRule: 'Heavy billboard advertising display contrasted with Google flagship neutral sans.',
    description: 'Maximum visual volume for music festivals, cinematic trailers, and athletic campaigns.'
  },
  {
    id: 'pair-bungee-karla',
    heading: 'Bungee',
    headingCat: 'Display',
    body: 'Karla',
    bodyCat: 'Sans-Serif',
    vibe: 'Creative & Bold',
    contrastRule: 'Urban vertical street signage caps grounded by quirky grotesque readability.',
    description: 'Vibrant pop-culture zines, skateboard apparel, and playful youth tech consumer apps.'
  },

  // 5. Classic Literary & Publishing
  {
    id: 'pair-garamond-cormorant',
    heading: 'EB Garamond',
    headingCat: 'Serif',
    body: 'Cormorant Infant',
    bodyCat: 'Serif',
    vibe: 'Classic Literary',
    contrastRule: 'Pure 16th-century Renaissance scholarly book hierarchy with delicate proportions.',
    description: 'Exquisite literary publishing standard for poetry collections, philosophy essays, and novels.'
  },
  {
    id: 'pair-lora-merriweathersans',
    heading: 'Lora',
    headingCat: 'Serif',
    body: 'Merriweather Sans',
    bodyCat: 'Sans-Serif',
    vibe: 'Classic Literary',
    contrastRule: 'Contemporary literary serif with complementary low-contrast sans-serif twin.',
    description: 'Warm humanist continuity ensuring fatigue-free digital reading over thousands of words.'
  },
  {
    id: 'pair-caslon-mulish',
    heading: 'Libre Caslon Display',
    headingCat: 'Serif',
    body: 'Mulish',
    bodyCat: 'Sans-Serif',
    vibe: 'Classic Literary',
    contrastRule: 'Timeless 18th-century British royal foundry serif paired with minimalist sans.',
    description: 'Commanding historical presence balanced by 21st-century mobile screen optimization.'
  },
  {
    id: 'pair-spectral-nunito',
    heading: 'Spectral',
    headingCat: 'Serif',
    body: 'Nunito Sans',
    bodyCat: 'Sans-Serif',
    vibe: 'Classic Literary',
    contrastRule: 'Screen-first Production Type reading serif paired with gentle rounded geometric sans.',
    description: 'Sophisticated long-form documentation, legal briefs, and educational course material.'
  },
  {
    id: 'pair-besley-worksans',
    heading: 'Besley',
    headingCat: 'Serif',
    body: 'Work Sans',
    bodyCat: 'Sans-Serif',
    vibe: 'Classic Literary',
    contrastRule: 'Early 19th-century Clarendon slab-serif punch anchored by warm grotesque sans.',
    description: 'Academic journals, historic preservation foundations, and museum research papers.'
  },

  // 6. Industrial & Developer Tools
  {
    id: 'pair-spacegrotesk-spacemono',
    heading: 'Space Grotesk',
    headingCat: 'Sans-Serif',
    body: 'Space Mono',
    bodyCat: 'Monospace',
    vibe: 'Developer & Industrial',
    contrastRule: 'Unified retro-computational aesthetic across proportional and fixed-width glyphs.',
    description: 'Cult favorite for hacker news aggregators, cybernetic zines, and crypto developer docs.'
  },
  {
    id: 'pair-firacode-firasans',
    heading: 'Fira Code',
    headingCat: 'Monospace',
    body: 'Fira Sans',
    bodyCat: 'Sans-Serif',
    vibe: 'Developer & Industrial',
    contrastRule: 'Mozilla unified developer system: programming ligatures paired with ergonomic sans.',
    description: 'Engineered specifically for technical documentation, API references, and code walkthroughs.'
  },
  {
    id: 'pair-ibm-serif-sans',
    heading: 'IBM Plex Serif',
    headingCat: 'Serif',
    body: 'IBM Plex Sans',
    bodyCat: 'Sans-Serif',
    vibe: 'Developer & Industrial',
    contrastRule: 'Engineered corporate rationality designed by Mike Abbink for IBM global brand identity.',
    description: 'The pinnacle of industrial design harmony: man vs. machine rendered in crisp typography.'
  },
  {
    id: 'pair-jetbrains-inter',
    heading: 'JetBrains Mono',
    headingCat: 'Monospace',
    body: 'Inter',
    bodyCat: 'Sans-Serif',
    vibe: 'Developer & Industrial',
    contrastRule: 'High-contrast code editor monospace headline paired with world-class UI sans.',
    description: 'The modern developer toolkit standard favored by GitHub, Supabase, and Warp terminal.'
  }
];

const vibes = [
  'All',
  'SaaS & Tech',
  'Editorial & Luxury',
  'Modern Minimalist',
  'Creative & Bold',
  'Classic Literary',
  'Developer & Industrial'
];

const pairingsJson = JSON.stringify(pairings);
---

<BaseLayout
  title="Font Pairing Generator — Typography Design System Studio"
  description="Generate designer-tested Google Font headline and body pairings. Test combinations live in an interactive design canvas with layout switchers and 1-click CSS exports."
>
  <!-- Preconnect and batch-load Google Fonts for initial top pairings -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600;700&family=Playfair+Display:ital,wght@0,600;0,700;1,400&family=Source+Sans+3:wght@400;500;600&family=Montserrat:wght@500;600;700&family=Lora:ital,wght@0,400;0,500;1,400&family=Plus+Jakarta+Sans:wght@500;600;700&family=Outfit:wght@400;500;600&family=Syne:wght@600;700;800&family=DM+Sans:wght@400;500;700&family=Cormorant+Garamond:ital,wght@0,600;0,700;1,400&family=Oswald:wght@500;600;700&family=Merriweather:wght@300;400;700&family=IBM+Plex+Serif:wght@500;600;700&family=IBM+Plex+Sans:wght@400;500;600&family=JetBrains+Mono:wght@500;600;700&display=swap"
  />

  <section class="relative pt-10 pb-16 sm:pt-14 sm:pb-24 px-4 sm:px-6 bg-[#08090a] bg-grid-pattern-subtle border-b border-[#23252a] min-h-[calc(100vh-140px)]">
    <div class="max-w-[1200px] mx-auto">
      
      <!-- Top Breadcrumb & Badge -->
      <div class="flex items-center justify-center gap-2 mb-3">
        <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-[#0f1011] border border-[#23252a]">
          <div class="w-1.5 h-1.5 rounded-full bg-[#e4f222]"></div>
          <a href="/tools" class="text-xs font-mono text-[#8a8f98] hover:text-[#ffffff] transition-colors">Tools</a>
          <span class="text-xs text-[#383b3f]">/</span>
          <span class="text-xs font-mono text-[#e4f222] uppercase tracking-wider">Design System Studio</span>
        </div>
      </div>

      <!-- Hero Header -->
      <div class="text-center max-w-3xl mx-auto">
        <h1 class="text-3xl sm:text-5xl font-medium text-[#ffffff] tracking-tight" style="letter-spacing: -0.022em;">
          Find a font pairing
        </h1>
        <p class="mt-3.5 text-sm sm:text-base text-[#8a8f98] leading-relaxed max-w-xl mx-auto">
          Typography harmony is an exact science. Test 31 designer-curated Google Font combinations across Editorial, SaaS Hero, and Product layouts with live CSS exports.
        </p>
      </div>

      <!-- ========================================================================= -->
      <!-- SECTION 1: INTERACTIVE DESIGN SYSTEM STUDIO (LIVE CANVAS) -->
      <!-- ========================================================================= -->
      <div id="studio" class="mt-10 rounded-[12px] border border-[#23252a] bg-[#0f1011] shadow-xs overflow-hidden">
        
        <!-- Studio Toolbar: Header Pair Status & Layout Switcher -->
        <div class="p-4 sm:p-5 border-b border-[#23252a] bg-[#161718] flex flex-col lg:flex-row items-start lg:items-center justify-between gap-4">
          
          <!-- Active Pair Display -->
          <div class="flex items-center gap-3">
            <div class="w-2.5 h-2.5 rounded-full bg-[#e4f222]"></div>
            <div>
              <div class="flex items-center gap-2">
                <span class="text-xs font-mono font-bold text-[#ffffff]" id="active-heading-name">Space Grotesk</span>
                <span class="text-xs font-mono text-[#e4f222]">&plus;</span>
                <span class="text-xs font-mono font-bold text-[#ffffff]" id="active-body-name">Inter</span>
                <span class="text-[10px] font-mono px-2 py-0.5 rounded-full bg-[rgba(228,242,34,0.1)] text-[#e4f222] border border-[rgba(228,242,34,0.2)] ml-2" id="active-vibe-badge">SaaS & Tech</span>
              </div>
              <p class="text-[11px] font-mono text-[#8a8f98] mt-0.5" id="active-rule-text">
                Neo-brutalist tech headline balanced by ultra-clean neutral UI workhorse.
              </p>
            </div>
          </div>

          <!-- Studio Action Buttons -->
          <div class="flex flex-wrap items-center gap-2 self-stretch sm:self-auto justify-end">
            <!-- Shuffle / Surprise Me Button -->
            <button
              type="button"
              id="btn-shuffle-pair"
              class="btn-ghost-linear px-3.5 py-1.5 rounded-[6px] text-xs font-mono transition-all cursor-pointer inline-flex items-center gap-1.5"
              title="Pick a random pairing"
            >
              <span>🎲</span>
              <span>Surprise Me</span>
            </button>

            <!-- Invert Roles (Swap Heading & Body) -->
            <button
              type="button"
              id="btn-swap-fonts"
              class="btn-ghost-linear px-3.5 py-1.5 rounded-[6px] text-xs font-mono transition-all cursor-pointer inline-flex items-center gap-1.5"
              title="Swap Heading and Body fonts"
            >
              <span>⇄</span>
              <span>Invert Roles</span>
            </button>

            <!-- Layout View Switchers -->
            <div class="inline-flex p-0.5 rounded-[6px] bg-[#0f1011] border border-[#23252a]">
              <button
                type="button"
                id="layout-btn-editorial"
                class="layout-view-btn px-2.5 py-1 rounded-[4px] text-xs font-mono transition-all bg-[#ffffff] text-[#08090a] font-medium shadow-xs cursor-pointer"
                data-layout="editorial"
              >
                Editorial
              </button>
              <button
                type="button"
                id="layout-btn-saas"
                class="layout-view-btn px-2.5 py-1 rounded-[4px] text-xs font-mono transition-all text-[#8a8f98] hover:text-[#ffffff] cursor-pointer"
                data-layout="saas"
              >
                SaaS Hero
              </button>
              <button
                type="button"
                id="layout-btn-product"
                class="layout-view-btn px-2.5 py-1 rounded-[4px] text-xs font-mono transition-all text-[#8a8f98] hover:text-[#ffffff] cursor-pointer"
                data-layout="product"
              >
                Product Card
              </button>
            </div>
          </div>
        </div>

        <!-- Studio Hierarchy & Sliders Control Panel -->
        <div class="px-5 py-3 border-b border-[#23252a] bg-[#0f1011] flex flex-wrap items-center justify-between gap-4 text-xs font-mono text-[#8a8f98]">
          <div class="flex flex-wrap items-center gap-6">
            <!-- Heading Size Slider -->
            <div class="flex items-center gap-2">
              <span>H1 Size:</span>
              <input
                id="slider-heading-size"
                type="range"
                min="24"
                max="60"
                value="38"
                class="w-24 accent-[#e4f222] cursor-pointer"
              />
              <span id="label-heading-size" class="w-8 text-[#ffffff] font-medium">38px</span>
            </div>

            <!-- Body Size Slider -->
            <div class="flex items-center gap-2">
              <span>Body Size:</span>
              <input
                id="slider-body-size"
                type="range"
                min="13"
                max="20"
                value="15"
                class="w-20 accent-[#e4f222] cursor-pointer"
              />
              <span id="label-body-size" class="w-8 text-[#ffffff] font-medium">15px</span>
            </div>

            <!-- Heading Weight -->
            <div class="flex items-center gap-2">
              <span>H1 Weight:</span>
              <select
                id="select-heading-weight"
                class="px-2 py-1 rounded-[4px] border border-[#23252a] bg-[#161718] text-xs text-[#ffffff] outline-none"
              >
                <option value="400">Regular (400)</option>
                <option value="600">SemiBold (600)</option>
                <option value="700" selected>Bold (700)</option>
                <option value="800">Black (800)</option>
              </select>
            </div>
          </div>

          <div class="text-[11px] text-[#8a8f98]">
            💡 All text below is <span class="text-[#e4f222]">editable</span> — click to test your own copy
          </div>
        </div>

        <!-- ========================================================================= -->
        <!-- LIVE CANVAS CONTENT AREA (3 Switchable Layouts) -->
        <!-- ========================================================================= -->
        <div class="p-6 sm:p-10 min-h-[380px] bg-[#0f1011] transition-colors">
          
          <!-- LAYOUT 1: EDITORIAL ARTICLE LAYOUT -->
          <div id="canvas-editorial" class="canvas-layout max-w-3xl mx-auto">
            <div class="flex items-center gap-2 mb-3">
              <span class="text-[10px] font-mono uppercase tracking-widest px-2 py-0.5 rounded-[4px] bg-[#161718] text-[#8a8f98] border border-[#23252a]">
                Typography Monograph
              </span>
              <span class="text-xs font-mono text-[#8a8f98]">&bull; Volume IV</span>
            </div>

            <h1
              id="live-heading-editorial"
              class="font-bold text-[#ffffff] tracking-tight outline-none transition-all leading-[1.15]"
              contenteditable="true"
              spellcheck="false"
              style="font-family: 'Space Grotesk', sans-serif; font-size: 38px; font-weight: 700;"
            >
              Architecture in digital spaces and the mathematical harmony of screens
            </h1>

            <div class="mt-4 pb-4 border-b border-[#23252a] flex items-center gap-3 text-xs font-mono text-[#8a8f98]">
              <span>By Elena Rostova</span>
              <span>&bull;</span>
              <span>6 min read</span>
              <span>&bull;</span>
              <span>Published in Architectural Review</span>
            </div>

            <h2
              id="live-subhead-editorial"
              class="mt-6 font-semibold text-[#d0d6e0] tracking-tight outline-none transition-all text-xl"
              contenteditable="true"
              spellcheck="false"
              style="font-family: 'Space Grotesk', sans-serif;"
            >
              Structuring contrast, optical baseline balance, and typographic pacing
            </h2>

            <p
              id="live-body-editorial-1"
              class="mt-4 text-[#d0d6e0] leading-relaxed outline-none transition-all"
              contenteditable="true"
              spellcheck="false"
              style="font-family: 'Inter', sans-serif; font-size: 15px;"
            >
              Good typography does not call attention to itself; it creates an invisible visual architecture that guides the reader effortlessly through complex hierarchies. When pairing two distinct typefaces, tension and harmony must balance with mathematical rhythm: the heading provides distinctive character and editorial voice, while the body typeface delivers effortless micro-legibility across screens.
            </p>

            <blockquote
              id="live-quote-editorial"
              class="my-6 pl-4 border-l-2 border-[#e4f222] italic text-[#ffffff] outline-none text-base transition-all"
              contenteditable="true"
              spellcheck="false"
              style="font-family: 'Space Grotesk', serif;"
            >
              "Typography is to writing as sheet music is to the performance. It dictates tempo, emotional cadence, and visual clarity."
            </blockquote>

            <p
              id="live-body-editorial-2"
              class="mt-4 text-[#d0d6e0] leading-relaxed outline-none transition-all"
              contenteditable="true"
              spellcheck="false"
              style="font-family: 'Inter', sans-serif; font-size: 15px;"
            >
              Notice how the x-heights and counter-shapes of this combination preserve typographic equilibrium. You can adjust the font size sliders in the toolbar above or choose another pairing from the curated catalog below to test instantly.
            </p>
          </div>

          <!-- LAYOUT 2: SAAS HERO LANDING LAYOUT -->
          <div id="canvas-saas" class="canvas-layout hidden max-w-3xl mx-auto text-center py-4">
            <div class="inline-flex items-center gap-2 mb-4 px-3 py-1 rounded-full bg-[rgba(228,242,34,0.1)] border border-[rgba(228,242,34,0.2)] text-[#e4f222] text-xs font-mono">
              <span class="w-1.5 h-1.5 rounded-full bg-[#e4f222]"></span>
              <span>NOW AVAILABLE FOR TEAMS &bull; VERSION 3.0</span>
            </div>

            <h1
              id="live-heading-saas"
              class="font-bold text-[#ffffff] tracking-tight outline-none transition-all leading-[1.15]"
              contenteditable="true"
              spellcheck="false"
              style="font-family: 'Space Grotesk', sans-serif; font-size: 42px; font-weight: 700;"
            >
              Ship high-converting digital products 10x faster with unified typography
            </h1>

            <p
              id="live-body-saas"
              class="mt-5 text-[#d0d6e0] leading-relaxed outline-none transition-all max-w-xl mx-auto"
              contenteditable="true"
              spellcheck="false"
              style="font-family: 'Inter', sans-serif; font-size: 16px;"
            >
              The developer-first typographic engine for building state-of-the-art web apps. Maintain design system fidelity from initial Figma tokens to production CSS.
            </p>

            <div class="mt-8 flex flex-wrap items-center justify-center gap-3">
              <button type="button" class="btn-acid-lime px-6 py-2.5 rounded-[6px] text-xs font-semibold shadow-xs transition-colors cursor-pointer">
                Start Building Free &rarr;
              </button>
              <button type="button" class="btn-ghost-linear px-5 py-2.5 rounded-[6px] text-xs font-mono transition-colors cursor-pointer">
                View API Documentation
              </button>
            </div>

            <!-- Mini Feature Stats Cards -->
            <div class="mt-10 grid grid-cols-1 sm:grid-cols-3 gap-3 text-left">
              <div class="p-4 rounded-[6px] border border-[#23252a] bg-[#161718]">
                <span class="text-xs font-mono text-[#e4f222]">99.99%</span>
                <h4 class="text-sm font-bold text-[#ffffff] mt-0.5" style="font-family: 'Space Grotesk', sans-serif;">Zero Layout Shift</h4>
                <p class="text-xs text-[#8a8f98] mt-1" style="font-family: 'Inter', sans-serif;">Optimized font-display swap.</p>
              </div>
              <div class="p-4 rounded-[6px] border border-[#23252a] bg-[#161718]">
                <span class="text-xs font-mono text-[#e4f222]">100% In-Browser</span>
                <h4 class="text-sm font-bold text-[#ffffff] mt-0.5" style="font-family: 'Space Grotesk', sans-serif;">Zero Latency</h4>
                <p class="text-xs text-[#8a8f98] mt-1" style="font-family: 'Inter', sans-serif;">Local Web Worker processing.</p>
              </div>
              <div class="p-4 rounded-[6px] border border-[#23252a] bg-[#161718]">
                <span class="text-xs font-mono text-[#e4f222]">Open License</span>
                <h4 class="text-sm font-bold text-[#ffffff] mt-0.5" style="font-family: 'Space Grotesk', sans-serif;">Commercial Free</h4>
                <p class="text-xs text-[#8a8f98] mt-1" style="font-family: 'Inter', sans-serif;">SIL Open Font License.</p>
              </div>
            </div>
          </div>

          <!-- LAYOUT 3: PRODUCT CARD LAYOUT -->
          <div id="canvas-product" class="canvas-layout hidden max-w-xl mx-auto py-4">
            <div class="p-6 rounded-[12px] border border-[#23252a] bg-[#161718] shadow-xs">
              <div class="flex items-center justify-between gap-2 mb-3">
                <span class="text-[10px] font-mono uppercase tracking-widest px-2 py-0.5 rounded-[4px] bg-[rgba(228,242,34,0.1)] text-[#e4f222] border border-[rgba(228,242,34,0.2)]">
                  Archival Edition
                </span>
                <span class="text-xs font-mono text-[#8a8f98]">Item #TK-0842</span>
              </div>

              <h2
                id="live-heading-product"
                class="font-bold text-[#ffffff] tracking-tight outline-none transition-all text-2xl sm:text-3xl"
                contenteditable="true"
                spellcheck="false"
                style="font-family: 'Space Grotesk', sans-serif;"
              >
                Chronograph Mechanical Watch No. 42
              </h2>

              <div class="mt-2 text-xl font-mono text-[#e4f222] font-semibold">
                $480.00 USD
              </div>

              <p
                id="live-body-product"
                class="mt-3 text-[#d0d6e0] leading-relaxed outline-none transition-all text-sm"
                contenteditable="true"
                spellcheck="false"
                style="font-family: 'Inter', sans-serif;"
              >
                Engineered from aircraft-grade titanium with high-domed scratch-resistant sapphire glass. Features a 38-jewel Swiss mechanical automatic movement calibrated for extreme precision.
              </p>

              <div class="mt-6 pt-4 border-t border-[#23252a] flex items-center justify-between gap-4">
                <div class="text-xs font-mono text-[#8a8f98]">
                  Free global express shipping &bull; 5-year warranty
                </div>
                <button type="button" class="btn-acid-lime px-5 py-2 rounded-[6px] text-xs font-mono font-medium transition-colors cursor-pointer shrink-0">
                  Add to Cart &rarr;
                </button>
              </div>
            </div>
          </div>

        </div>

        <!-- ========================================================================= -->
        <!-- BOTTOM BAR: PRODUCTION CSS & DESIGN SYSTEM TOKENS EXPORT DRAWER -->
        <!-- ========================================================================= -->
        <div class="p-5 border-t border-[#23252a] bg-[#161718]">
          <div class="flex flex-col md:flex-row items-start md:items-center justify-between gap-4">
            
            <div class="flex items-center gap-3">
              <span class="text-xs font-mono font-bold text-[#ffffff]">Production Code:</span>
              
              <!-- Code Format Toggle -->
              <div class="inline-flex p-0.5 rounded-[6px] bg-[#0f1011] border border-[#23252a]">
                <button type="button" id="btn-export-css" class="btn-export-type px-2.5 py-1 rounded-[4px] text-[11px] font-mono bg-[#ffffff] text-[#08090a] font-medium shadow-xs cursor-pointer" data-type="css">
                  CSS Variables
                </button>
                <button type="button" id="btn-export-import" class="btn-export-type px-2.5 py-1 rounded-[4px] text-[11px] font-mono text-[#8a8f98] hover:text-[#ffffff] cursor-pointer" data-type="import">
                  Google @import
                </button>
                <button type="button" id="btn-export-tailwind" class="btn-export-type px-2.5 py-1 rounded-[4px] text-[11px] font-mono text-[#8a8f98] hover:text-[#ffffff] cursor-pointer" data-type="tailwind">
                  Tailwind Config
                </button>
              </div>
            </div>

            <!-- Copy Code Button -->
            <button
              type="button"
              id="btn-copy-active-code"
              class="btn-acid-lime px-5 py-2 rounded-[6px] text-xs font-mono font-medium transition-all shadow-xs inline-flex items-center gap-2 cursor-pointer"
            >
              <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 5H6a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2v-1M8 5a2 2 0 002 2h2a2 2 0 002-2M8 5a2 2 0 012-2h2a2 2 0 012 2m0 0h2a2 2 0 012 2v3m2 4H10m0 0l3-3m-3 3l3 3" />
              </svg>
              <span>Copy Production Code</span>
            </button>
          </div>

          <!-- Code Display Block -->
          <pre id="active-code-display" class="mt-3 p-3.5 rounded-[6px] bg-[#0f1011] border border-[#23252a] text-[11px] font-mono text-[#d0d6e0] overflow-x-auto leading-relaxed"></pre>
        </div>

      </div>

      <!-- ========================================================================= -->
      <!-- SECTION 2: CURATED PAIRINGS DIRECTORY & FILTER ENGINE (31 Pairs) -->
      <!-- ========================================================================= -->
      <div class="mt-14">
        <div class="flex flex-col sm:flex-row sm:items-end justify-between gap-4 pb-5 border-b border-[#23252a]">
          <div>
            <div class="flex items-center gap-2 mb-1">
              <div class="w-1.5 h-1.5 rounded-full bg-[#e4f222]"></div>
              <span class="font-mono text-xs uppercase tracking-wider text-[#8a8f98]">EXPLORE MATRIX</span>
            </div>
            <h2 class="text-2xl font-bold text-[#ffffff] tracking-tight">
              Curated typography pairings directory
            </h2>
            <p class="text-xs text-[#8a8f98] mt-1">
              Click "Test in Studio" on any pairing to load it into the interactive live canvas above.
            </p>
          </div>

          <!-- Search Filter -->
          <div class="relative w-full sm:w-72">
            <input
              id="pairing-search"
              type="text"
              placeholder="Search by font name or vibe..."
              class="w-full pl-9 pr-4 py-2 rounded-[6px] border border-[#23252a] bg-[#0f1011] text-xs font-mono text-[#ffffff] focus:outline-none focus:border-[#d0d6e0] placeholder:text-[#8a8f98] transition-all"
            />
            <svg class="w-4 h-4 text-[#8a8f98] absolute left-3 top-2.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
            </svg>
          </div>
        </div>

        <!-- Vibe Filter Chips -->
        <div class="mt-5 flex flex-wrap items-center gap-1.5" id="vibe-filters">
          {vibes.map((vibe, idx) => (
            <button
              type="button"
              data-vibe={vibe}
              class={`vibe-btn text-xs font-mono px-3.5 py-1.5 rounded-full border transition-all cursor-pointer ${
                idx === 0
                  ? 'bg-[#ffffff] text-[#08090a] border-transparent font-medium'
                  : 'bg-[#161718] border-[#23252a] text-[#8a8f98] hover:border-[#e4f222] hover:text-[#ffffff]'
              }`}
            >
              {vibe}
            </button>
          ))}
        </div>

        <!-- Status Count -->
        <div class="mt-5 flex items-center justify-between text-xs font-mono text-[#8a8f98]">
          <span>Showing <strong id="pair-count" class="text-[#e4f222]">31</strong> pairings</span>
          <span class="text-[11px]">100% Free Google Fonts &bull; Open Source</span>
        </div>

        <!-- Grid of Pair Cards -->
        <div id="pairings-container" class="mt-4 grid grid-cols-1 md:grid-cols-2 gap-5">
          {pairings.map((p) => {
            const encodedHeading = encodeURIComponent(p.heading).replace(/%20/g, '+');
            const encodedBody = encodeURIComponent(p.body).replace(/%20/g, '+');
            return (
              <div
                class="pair-card p-6 rounded-[12px] border border-[#23252a] bg-[#0f1011] shadow-xs hover:border-[#383b3f] transition-all flex flex-col justify-between group"
                data-id={p.id}
                data-heading={p.heading}
                data-heading-cat={p.headingCat}
                data-body={p.body}
                data-body-cat={p.bodyCat}
                data-vibe={p.vibe}
                data-rule={p.contrastRule}
              >
                <div>
                  <!-- Top Row -->
                  <div class="flex items-center justify-between gap-3 pb-3 border-b border-[#23252a]">
                    <span class="text-[11px] font-mono px-2.5 py-0.5 rounded-[4px] bg-[#161718] text-[#d0d6e0] border border-[#23252a]">
                      {p.vibe}
                    </span>

                    <button
                      type="button"
                      class="btn-load-studio text-[11px] font-mono text-[#e4f222] hover:underline cursor-pointer inline-flex items-center gap-1"
                      data-id={p.id}
                    >
                      <span>Test in Studio</span>
                      <span>&uarr;</span>
                    </button>
                  </div>

                  <!-- Font Pairing Title Names -->
                  <div class="mt-3 flex items-center gap-2">
                    <h3 class="text-base font-bold text-[#ffffff] tracking-tight">
                      {p.heading}
                    </h3>
                    <span class="text-xs font-mono text-[#e4f222] font-bold">&plus;</span>
                    <h3 class="text-base font-bold text-[#ffffff] tracking-tight">
                      {p.body}
                    </h3>
                  </div>

                  <!-- Contrast Rule Note -->
                  <p class="mt-1 text-xs text-[#8a8f98] leading-relaxed">
                    {p.contrastRule}
                  </p>

                  <!-- Live Mini Preview -->
                  <div class="my-5 p-4 rounded-[6px] bg-[#161718] border border-[#23252a]">
                    <div
                      class="text-xl font-bold text-[#ffffff] tracking-tight leading-tight"
                      style={`font-family: '${p.heading}', serif;`}
                    >
                      Typography creates visual rhythm
                    </div>
                    <div
                      class="mt-2 text-xs text-[#d0d6e0] leading-relaxed line-clamp-2"
                      style={`font-family: '${p.body}', sans-serif;`}
                    >
                      {p.description}
                    </div>
                  </div>
                </div>

                <!-- Bottom Bar: Actions & Links -->
                <div class="pt-3.5 border-t border-[#23252a] flex items-center justify-between gap-3">
                  <button
                    type="button"
                    data-css={`/* Design System Typography Tokens */\n:root {\n  --font-heading: '${p.heading}', ${p.headingCat.toLowerCase()};\n  --font-body: '${p.body}', ${p.bodyCat.toLowerCase()};\n}`}
                    class="btn-copy-card-css btn-ghost-linear px-3 py-1.5 rounded-[6px] text-[11px] font-mono font-medium cursor-pointer"
                  >
                    Copy CSS
                  </button>

                  <div class="flex items-center gap-3 text-xs font-mono">
                    <a
                      href={`https://fonts.google.com/specimen/${p.heading.replace(/\s+/g, '+')}`}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="text-[#8a8f98] hover:text-[#e4f222] transition-colors"
                    >
                      {p.heading} &rarr;
                    </a>
                    <a
                      href={`https://fonts.google.com/specimen/${p.body.replace(/\s+/g, '+')}`}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="text-[#8a8f98] hover:text-[#e4f222] transition-colors"
                    >
                      {p.body} &rarr;
                    </a>
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        <div id="no-pairings" class="hidden py-16 text-center text-xs font-mono text-[#8a8f98] border border-dashed border-[#23252a] rounded-[12px] mt-4">
          No pairings matched your search. Try "Space Grotesk", "Playfair", "Inter", or select "All".
        </div>
      </div>

      <!-- ========================================================================= -->
      <!-- SECTION 3: TYPOGRAPHIC PAIRING SCIENCE & RULES -->
      <!-- ========================================================================= -->
      <div class="mt-16 p-8 rounded-[12px] border border-[#23252a] bg-[#0f1011] shadow-xs">
        <h3 class="text-lg font-bold text-[#ffffff] tracking-tight">
          The 3 Golden Rules of Typographic Pairing
        </h3>
        <p class="mt-2 text-xs sm:text-sm text-[#8a8f98] leading-relaxed max-w-2xl">
          Pairing fonts is about establishing clear contrast without conflict. Industry typographers follow three fundamental rules:
        </p>

        <div class="mt-6 grid grid-cols-1 md:grid-cols-3 gap-5">
          <div class="p-5 rounded-[6px] border border-[#23252a] bg-[#161718]">
            <span class="text-xs font-mono text-[#e4f222] font-semibold block mb-1">RULE 01</span>
            <h4 class="text-sm font-bold text-[#ffffff]">Category Contrast</h4>
            <p class="mt-1.5 text-xs text-[#8a8f98] leading-relaxed">
              Pair typefaces from different historical classifications (e.g. Serif Header with Sans-Serif Body). Never pair two fonts that look almost identical, as it creates optical discord.
            </p>
          </div>

          <div class="p-5 rounded-[6px] border border-[#23252a] bg-[#161718]">
            <span class="text-xs font-mono text-[#e4f222] font-semibold block mb-1">RULE 02</span>
            <h4 class="text-sm font-bold text-[#ffffff]">Proportional x-Height Harmony</h4>
            <p class="mt-1.5 text-xs text-[#8a8f98] leading-relaxed">
              Ensure both fonts share a visually compatible x-height ratio. Fonts with vastly differing baseline proportions feel disjointed when rendered on the same screen.
            </p>
          </div>

          <div class="p-5 rounded-[6px] border border-[#23252a] bg-[#161718]">
            <span class="text-xs font-mono text-[#e4f222] font-semibold block mb-1">RULE 03</span>
            <h4 class="text-sm font-bold text-[#ffffff]">Distinct Role Assignment</h4>
            <p class="mt-1.5 text-xs text-[#8a8f98] leading-relaxed">
              Assign one expressive typeface for headlines (the "personality") and one clean, neutral workhorse for body text (the "clarity"). Never use two expressive fonts together.
            </p>
          </div>
        </div>
        </div>
  </section>

  <!-- Client-Side Pairings Data & Logic Engine -->
  <script id="pairings-data" type="application/json" is:inline set:html={pairingsJson}></script>
  <script is:inline>
    (function () {
      const pairingsData = JSON.parse(document.getElementById('pairings-data')?.textContent || '[]');
      
      // Studio DOM Elements
      const activeHeadingName = document.getElementById('active-heading-name');
      const activeBodyName = document.getElementById('active-body-name');
      const activeVibeBadge = document.getElementById('active-vibe-badge');
      const activeRuleText = document.getElementById('active-rule-text');
      const activeCodeDisplay = document.getElementById('active-code-display');
      const btnCopyActiveCode = document.getElementById('btn-copy-active-code');
      const btnShuffle = document.getElementById('btn-shuffle-pair');
      const btnSwap = document.getElementById('btn-swap-fonts');

      // Sliders & Weight Select
      const sliderHeadingSize = document.getElementById('slider-heading-size');
      const sliderBodySize = document.getElementById('slider-body-size');
      const labelHeadingSize = document.getElementById('label-heading-size');
      const labelBodySize = document.getElementById('label-body-size');
      const selectHeadingWeight = document.getElementById('select-heading-weight');

      // Layout Views
      const layoutButtons = document.querySelectorAll('.layout-view-btn');
      const canvasEditorial = document.getElementById('canvas-editorial');
      const canvasSaas = document.getElementById('canvas-saas');
      const canvasProduct = document.getElementById('canvas-product');

      // Export Type Buttons
      const exportButtons = document.querySelectorAll('.btn-export-type');

      // Live Elements in Canvas
      const headingElements = [
        document.getElementById('live-heading-editorial'),
        document.getElementById('live-subhead-editorial'),
        document.getElementById('live-heading-saas'),
        document.getElementById('live-heading-product')
      ];

      const bodyElements = [
        document.getElementById('live-body-editorial-1'),
        document.getElementById('live-body-editorial-2'),
        document.getElementById('live-quote-editorial'),
        document.getElementById('live-body-saas'),
        document.getElementById('live-body-product')
      ];

      // Directory Search & Filter Elements
      const searchInput = document.getElementById('pairing-search');
      const vibeButtons = document.querySelectorAll('.vibe-btn');
      const cards = document.querySelectorAll('.pair-card');
      const pairCount = document.getElementById('pair-count');
      const noResults = document.getElementById('no-pairings');

      // State
      let currentPairIndex = 0;
      let activeHeading = pairingsData[0]?.heading || 'Space Grotesk';
      let activeHeadingCat = pairingsData[0]?.headingCat || 'sans-serif';
      let activeBody = pairingsData[0]?.body || 'Inter';
      let activeBodyCat = pairingsData[0]?.bodyCat || 'sans-serif';
      let activeVibe = pairingsData[0]?.vibe || 'SaaS & Tech';
      let activeRule = pairingsData[0]?.contrastRule || '';
      let activeExportType = 'css';

      let directoryFilterVibe = 'All';
      let directorySearchQuery = '';

      // Load Google Font Dynamically into Head
      function ensureFontLoaded(fontName) {
        const id = `gfont-${fontName.replace(/\s+/g, '-').toLowerCase()}`;
        if (!document.getElementById(id)) {
          const link = document.createElement('link');
          link.id = id;
          link.rel = 'stylesheet';
          link.href = `https://fonts.googleapis.com/css2?family=${encodeURIComponent(fontName).replace(/%20/g, '+')}:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap`;
          document.head.appendChild(link);
        }
      }

      // Generate Production Export Code Snippet
      function generateCodeSnippet(h, hCat, b, bCat, type) {
        const encodedH = encodeURIComponent(h).replace(/%20/g, '+');
        const encodedB = encodeURIComponent(b).replace(/%20/g, '+');

        if (type === 'import') {
          return `@import url('https://fonts.googleapis.com/css2?family=${encodedH}:wght@600;700&family=${encodedB}:wght@400;500;600&display=swap');`;
        } else if (type === 'tailwind') {
          return `// tailwind.config.js\nmodule.exports = {\n  theme: {\n    extend: {\n      fontFamily: {\n        heading: ["'${h}'", "${hCat.toLowerCase()}"],\n        body: ["'${b}'", "${bCat.toLowerCase()}"]\n      }\n    }\n  }\n};`;
        } else {
          return `/* Design System CSS Variables */\n:root {\n  --font-heading: '${h}', ${hCat.toLowerCase()};\n  --font-body: '${b}', ${bCat.toLowerCase()};\n}\n\nh1, h2, h3, h4 {\n  font-family: var(--font-heading);\n}\n\nbody, p, input, button {\n  font-family: var(--font-body);\n}`;
        }
      }

      // Update Studio Elements
      function applyPairToStudio(pair) {
        activeHeading = pair.heading;
        activeHeadingCat = pair.headingCat;
        activeBody = pair.body;
        activeBodyCat = pair.bodyCat;
        activeVibe = pair.vibe;
        activeRule = pair.contrastRule;

        ensureFontLoaded(activeHeading);
        ensureFontLoaded(activeBody);

        if (activeHeadingName) activeHeadingName.textContent = activeHeading;
        if (activeBodyName) activeBodyName.textContent = activeBody;
        if (activeVibeBadge) activeVibeBadge.textContent = activeVibe;
        if (activeRuleText) activeRuleText.textContent = activeRule;

        // Apply to Headings
        headingElements.forEach((el) => {
          if (el) el.style.fontFamily = `'${activeHeading}', ${activeHeadingCat.toLowerCase()}`;
        });

        // Apply to Body
        bodyElements.forEach((el) => {
          if (el) el.style.fontFamily = `'${activeBody}', ${activeBodyCat.toLowerCase()}`;
        });

        // Update Code Block
        if (activeCodeDisplay) {
          activeCodeDisplay.textContent = generateCodeSnippet(activeHeading, activeHeadingCat, activeBody, activeBodyCat, activeExportType);
        }
      }

      // Initialize Studio with first pair
      if (pairingsData.length > 0) {
        applyPairToStudio(pairingsData[0]);
      }

      // Swap Heading and Body
      btnSwap?.addEventListener('click', () => {
        const tempH = activeHeading;
        const tempHCat = activeHeadingCat;
        activeHeading = activeBody;
        activeHeadingCat = activeBodyCat;
        activeBody = tempH;
        activeBodyCat = tempHCat;

        applyPairToStudio({
          heading: activeHeading,
          headingCat: activeHeadingCat,
          body: activeBody,
          bodyCat: activeBodyCat,
          vibe: activeVibe,
          contrastRule: `Inverted Role: ${activeHeading} for titles, ${activeBody} for body.`
        });
      });

      // Shuffle / Surprise Me
      btnShuffle?.addEventListener('click', () => {
        let nextIndex;
        do {
          nextIndex = Math.floor(Math.random() * pairingsData.length);
        } while (nextIndex === currentPairIndex && pairingsData.length > 1);

        currentPairIndex = nextIndex;
        applyPairToStudio(pairingsData[currentPairIndex]);
      });

      // Sliders & Weight
      sliderHeadingSize?.addEventListener('input', (e) => {
        const val = e.target.value;
        if (labelHeadingSize) labelHeadingSize.textContent = `${val}px`;
        const h1 = document.getElementById('live-heading-editorial');
        const hSaas = document.getElementById('live-heading-saas');
        if (h1) h1.style.fontSize = `${val}px`;
        if (hSaas) hSaas.style.fontSize = `${Math.min(val * 1.1, 56)}px`;
      });

      sliderBodySize?.addEventListener('input', (e) => {
        const val = e.target.value;
        if (labelBodySize) labelBodySize.textContent = `${val}px`;
        bodyElements.forEach((el) => {
          if (el && el.id !== 'live-quote-editorial') {
            el.style.fontSize = `${val}px`;
          }
        });
      });

      selectHeadingWeight?.addEventListener('change', (e) => {
        const val = e.target.value;
        headingElements.forEach((el) => {
          if (el) el.style.fontWeight = val;
        });
      });

      // Layout Switcher
      layoutButtons.forEach((btn) => {
        btn.addEventListener('click', () => {
          layoutButtons.forEach((b) => {
            b.className = 'layout-view-btn px-2.5 py-1 rounded-[4px] text-xs font-mono transition-all text-[#8a8f98] hover:text-[#ffffff] cursor-pointer';
          });
          btn.className = 'layout-view-btn px-2.5 py-1 rounded-[4px] text-xs font-mono transition-all bg-[#ffffff] text-[#08090a] font-medium shadow-xs cursor-pointer';

          const layout = btn.getAttribute('data-layout');
          if (layout === 'editorial') {
            canvasEditorial?.classList.remove('hidden');
            canvasSaas?.classList.add('hidden');
            canvasProduct?.classList.add('hidden');
          } else if (layout === 'saas') {
            canvasSaas?.classList.remove('hidden');
            canvasEditorial?.classList.add('hidden');
            canvasProduct?.classList.add('hidden');
          } else if (layout === 'product') {
            canvasProduct?.classList.remove('hidden');
            canvasEditorial?.classList.add('hidden');
            canvasProduct?.classList.add('hidden');
          }
        });
      });

      // Export Type Buttons
      exportButtons.forEach((btn) => {
        btn.addEventListener('click', () => {
          exportButtons.forEach((b) => {
            b.className = 'btn-export-type px-2.5 py-1 rounded-[4px] text-[11px] font-mono text-[#8a8f98] hover:text-[#ffffff] cursor-pointer';
          });
          btn.className = 'btn-export-type px-2.5 py-1 rounded-[4px] text-[11px] font-mono bg-[#ffffff] text-[#08090a] font-medium shadow-xs cursor-pointer';

          activeExportType = btn.getAttribute('data-type') || 'css';
          if (activeCodeDisplay) {
            activeCodeDisplay.textContent = generateCodeSnippet(activeHeading, activeHeadingCat, activeBody, activeBodyCat, activeExportType);
          }
        });
      });

      // Copy Active Code
      btnCopyActiveCode?.addEventListener('click', () => {
        const text = activeCodeDisplay?.textContent || '';
        navigator.clipboard.writeText(text).then(() => {
          const original = btnCopyActiveCode.innerHTML;
          btnCopyActiveCode.innerHTML = '<span>✓ Copied!</span>';
          setTimeout(() => {
            btnCopyActiveCode.innerHTML = original;
          }, 1800);
        });
      });

      // Load Pairing from Directory Card into Studio
      document.querySelectorAll('.btn-load-studio').forEach((btn) => {
        btn.addEventListener('click', () => {
          const id = btn.getAttribute('data-id');
          const pair = pairingsData.find((p) => p.id === id);
          if (pair) {
            applyPairToStudio(pair);
            document.getElementById('studio')?.scrollIntoView({ behavior: 'smooth', block: 'start' });
          }
        });
      });

      // Copy Card CSS button
      document.querySelectorAll('.btn-copy-card-css').forEach((btn) => {
        btn.addEventListener('click', () => {
          const css = btn.getAttribute('data-css') || '';
          navigator.clipboard.writeText(css).then(() => {
            const original = btn.textContent;
            btn.textContent = 'Copied!';
            btn.classList.add('bg-[#e4f222]', 'text-[#08090a]');
            setTimeout(() => {
              btn.textContent = original;
              btn.classList.remove('bg-[#e4f222]', 'text-[#08090a]');
            }, 1800);
          });
        });
      });

      // Directory Search and Vibe Filter Logic
      function filterDirectory() {
        let visible = 0;
        cards.forEach((card) => {
          const heading = (card.getAttribute('data-heading') || '').toLowerCase();
          const body = (card.getAttribute('data-body') || '').toLowerCase();
          const vibe = card.getAttribute('data-vibe') || '';

          const matchesVibe = directoryFilterVibe === 'All' || vibe === directoryFilterVibe;
          const matchesSearch = !directorySearchQuery || heading.includes(directorySearchQuery) || body.includes(directorySearchQuery) || vibe.toLowerCase().includes(directorySearchQuery);

          if (matchesVibe && matchesSearch) {
            card.classList.remove('hidden');
            visible++;
          } else {
            card.classList.add('hidden');
          }
        });

        if (pairCount) pairCount.textContent = String(visible);
        if (noResults) {
          if (visible === 0) noResults.classList.remove('hidden');
          else noResults.classList.add('hidden');
        }
      }

      searchInput?.addEventListener('input', (e) => {
        directorySearchQuery = e.target.value.trim().toLowerCase();
        filterDirectory();
      });

      vibeButtons.forEach((btn) => {
        btn.addEventListener('click', () => {
          vibeButtons.forEach((b) => {
            b.className = 'vibe-btn text-xs font-mono px-3.5 py-1.5 rounded-full border transition-all cursor-pointer bg-[#161718] border-[#23252a] text-[#8a8f98] hover:border-[#e4f222] hover:text-[#ffffff]';
          });
          btn.className = 'vibe-btn text-xs font-mono px-3.5 py-1.5 rounded-full border transition-all cursor-pointer bg-[#ffffff] text-[#08090a] border-transparent font-medium';
          directoryFilterVibe = btn.getAttribute('data-vibe') || 'All';
          filterDirectory();
        });
      });
    })();
  </script>
</BaseLayout>
